# ── Core RAG / ingestion (Week 1) ──
qdrant-client==1.12.1
sentence-transformers==2.7.0      # serves BAAI/bge-base-en-v1.5
torch>=2.0.0                       # CPU build is fine on Apple Silicon (MPS)
numpy<2.0                          # 2.x can break older torch/st wheels

# ── Backend API (Week 4; imported early for schemas) ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9           # file uploads

# ── LLM client (Week 2) ──
ollama==0.3.3                      # local LLM HTTP client

# ── Frontend (Week 4) ──
streamlit==1.39.0
requests==2.32.3

# ── Eval (Week 5) ──
mlflow==2.16.2

# ── Dev / notebooks ──
jupyter==1.1.1
